# WORKFLOW: /gdd-update

# Триггер: /gdd-update

# Агент: GD

1. Обновить `docs/GDD/GDD_[Name]_v[N].md` (инкрементировать версию!)
2. Записать diff в `decisions_log.md`:

   ```
   ## GDD UPDATE v[N] — [Дата]
   Изменено: [что именно]
   Причина: [почему]
   Влияние DEV: [нужны изменения в коде?]
   Влияние ART: [нужны новые ассеты?]
   Влияние UIUX: [нужны новые экраны?]
   ```

3. Обновить `gdd_summary.md` (макс. 500 слов)
4. Уведомить PM — PM перераспределяет задачи
